---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: clusterfqdnnetworkpolicies.netsec.kunal.dev
spec:
  group: netsec.kunal.dev
  names:
    kind: ClusterFQDNNetworkPolicy
    listKind: ClusterFQDNNetworkPolicyList
    plural: clusterfqdnnetworkpolicies
    shortNames:
    - cfqdnnp
    singular: clusterfqdnnetworkpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.affectedNamespaces
      name: Namespaces
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterFQDNNetworkPolicy enforces FQDN-based egress rules across all namespaces
          matching a selector. The controller creates one NetworkPolicy per matched namespace
          and garbage-collects it if the namespace no longer matches.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated. In CamelCase.
            type: string
          metadata:
            type: object
          spec:
            description: ClusterFQDNNetworkPolicySpec defines the desired state.
            properties:
              coreDNSAddress:
                description: |-
                  CoreDNSAddress is the host:port of the cluster DNS server to query directly
                  (e.g. "10.96.0.10:53").
                type: string
              egress:
                description: Egress is the list of allowed external FQDNs.
                items:
                  description: FQDNRule describes one allowed external destination.
                  properties:
                    match:
                      description: Match is a hostname or wildcard pattern.
                      minLength: 1
                      type: string
                    ports:
                      description: Ports restricts the rule to specific destination ports/protocols.
                      items:
                        description: PolicyPort mirrors networkingv1.NetworkPolicyPort.
                        properties:
                          port:
                            description: Port is the destination port number (1-65535).
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          protocol:
                            default: TCP
                            description: Protocol is TCP, UDP, or SCTP.
                            enum:
                            - TCP
                            - UDP
                            - SCTP
                            type: string
                        required:
                        - port
                        type: object
                      type: array
                  required:
                  - match
                  type: object
                minItems: 1
                type: array
              mode:
                default: Enforce
                description: |-
                  Mode controls whether the controller writes NetworkPolicies (Enforce)
                  or only logs resolved IPs (Audit). Defaults to Enforce.
                enum:
                - Enforce
                - Audit
                type: string
              namespaceSelector:
                description: |-
                  NamespaceSelector selects the namespaces this policy applies to.
                  An empty selector targets all namespaces.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                    items:
                      description: A label selector requirement.
                      properties:
                        key:
                          description: key is the label key that the selector applies to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to a set of values.
                          type: string
                        values:
                          description: values is an array of string values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              podSelector:
                description: PodSelector targets which pods in the matched namespaces the rules apply to.
                properties:
                  podSelector:
                    description: |-
                      PodSelector selects the pods this policy applies to.
                      Empty selector means "all pods".
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector requirements.
                        items:
                          description: A label selector requirement.
                          properties:
                            key:
                              description: key is the label key that the selector applies to.
                              type: string
                            operator:
                              description: operator represents a key's relationship to a set of values.
                              type: string
                            values:
                              description: values is an array of string values.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: matchLabels is a map of {key,value} pairs.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              resolutionTTLOverride:
                description: ResolutionTTLOverride forces a re-resolution interval in seconds (5-300).
                format: int32
                maximum: 300
                minimum: 5
                type: integer
            required:
            - egress
            - podSelector
            type: object
          status:
            description: ClusterFQDNNetworkPolicyStatus defines the observed state.
            properties:
              affectedNamespaces:
                description: AffectedNamespaces lists the namespaces currently receiving a NetworkPolicy from this resource.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions follow the standard metav1.Condition pattern.
                items:
                  description: Condition contains details for one aspect of the current state.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation the condition was set upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration lets us detect stale status vs spec.
                format: int64
                type: integer
              resolvedHosts:
                description: ResolvedHosts is the current resolution cache.
                items:
                  description: ResolvedHost records the last known IPs for one matched hostname.
                  properties:
                    hostname:
                      type: string
                    ips:
                      items:
                        type: string
                      type: array
                    lastSeen:
                      format: date-time
                      type: string
                    source:
                      type: string
                    ttlSeconds:
                      format: int32
                      type: integer
                  required:
                  - hostname
                  - ips
                  - lastSeen
                  - source
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
